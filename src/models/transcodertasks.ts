import {Schema, model} from 'mongoose';

/**
 * contains the schema for the transcodertasks
 * TODO: complete the schema
 */
let transcodertasks_Schema: Schema = new Schema({
});

export default model('transcodertasks', transcodertasks_Schema);