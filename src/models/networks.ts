import {Schema, model} from 'mongoose';

/**
 * contains the schema for the networks
 * TODO: complete the schema
 */
let networks_Schema: Schema = new Schema({
});

export default model('networks', networks_Schema);