import {Schema, model} from 'mongoose';

/**
 * contains the schema for the transcodertemplates
 * TODO: complete the schema
 */
let transcodertemplates_Schema: Schema = new Schema({
});

export default model('transcodertemplates', transcodertemplates_Schema);