import {Schema, model} from 'mongoose';

/**
 * contains the schema for the mediaboardtemplates
 * TODO: complete the schema
 */
let mediaboardtemplates_Schema: Schema = new Schema({
});

export default model('mediaboardtemplates', mediaboardtemplates_Schema);