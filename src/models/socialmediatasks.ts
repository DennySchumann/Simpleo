import {Schema, model} from 'mongoose';

/**
 * contains the schema for the socialmediatasks
 * TODO: complete the schema
 */
let socialmediatasks_Schema: Schema = new Schema({
});

export default model('socialmediatasks', socialmediatasks_Schema);