import {Schema, model} from 'mongoose';

/**
 * contains the schema for the userroles
 * TODO: complete the schema
 */
let userroles_Schema: Schema = new Schema({
});

export default model('userroles', userroles_Schema);