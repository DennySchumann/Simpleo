import {Schema, model} from 'mongoose';

/**
 * contains the schema for the media
 * TODO: complete the schema
 */
let media_Schema: Schema = new Schema({
});

export default model('media', media_Schema);