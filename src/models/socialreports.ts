import {Schema, model} from 'mongoose';

/**
 * contains the schema for the socialreports
 * TODO: complete the schema
 */
let socialreports_Schema: Schema = new Schema({
});

export default model('socialreports', socialreports_Schema);