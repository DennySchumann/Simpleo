import {Schema, model} from 'mongoose';

/**
 * contains the schema for the vodtemps
 * TODO: complete the schema
 */
let vodtemps_Schema: Schema = new Schema({
});

export default model('vodtemps', vodtemps_Schema);