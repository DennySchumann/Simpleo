import {Schema, model} from 'mongoose';

/**
 * contains the schema for the mediaboards
 * TODO: complete the schema
 */
let mediaboards_Schema: Schema = new Schema({
});

export default model('mediaboards', mediaboards_Schema);