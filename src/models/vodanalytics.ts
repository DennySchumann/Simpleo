import {Schema, model} from 'mongoose';

/**
 * contains the schema for the vodanalytics
 * TODO: complete the schema
 */
let vodanalytics_Schema: Schema = new Schema({
});

export default model('vodanalytics', vodanalytics_Schema);