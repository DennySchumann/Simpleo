import {Schema, model} from 'mongoose';

/**
 * contains the schema for the reports
 * TODO: complete the schema
 */
let reports_Schema: Schema = new Schema({
});

export default model('reports', reports_Schema);