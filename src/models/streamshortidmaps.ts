import {Schema, model} from 'mongoose';

/**
 * contains the schema for the streamshortidmaps
 * TODO: complete the schema
 */
let streamshortidmaps_Schema: Schema = new Schema({
});

export default model('streamshortidmaps', streamshortidmaps_Schema);