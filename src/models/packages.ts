import {Schema, model} from 'mongoose';

/**
 * contains the schema for the packages
 * TODO: complete the schema
 */
let packages_Schema: Schema = new Schema({
});

export default model('packages', packages_Schema);