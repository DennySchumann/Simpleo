import {Schema, model} from 'mongoose';

/**
 * contains the schema for the admap
 * TODO: complete the schema
 */
let admaps_Schema: Schema = new Schema({
});

export default model('admaps', admaps_Schema);