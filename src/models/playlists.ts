import {Schema, model} from 'mongoose';

/**
 * contains the schema for the playlists
 * TODO: complete the schema
 */
let playlists_Schema: Schema = new Schema({
});

export default model('playlists', playlists_Schema);