import {Schema, model} from 'mongoose';

/**
 * contains the schema for the liveanalytics
 * TODO: complete the schema
 */
let liveanalytics_Schema: Schema = new Schema({
});

export default model('liveanalytics', liveanalytics_Schema);