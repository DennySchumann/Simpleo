import {Schema, model} from 'mongoose';

/**
 * contains the schema for the vouchers
 * TODO: complete the schema
 */
let vouchers_Schema: Schema = new Schema({
});

export default model('vouchers', vouchers_Schema);